{
  "compilerOptions": {
    "baseUrl": "./src",   // 设置 baseUrl 为 src 目录
    "paths": {
      "@/*": ["src/*"]     // 将 @ 映射到 src 目录
    },
    "jsx": "react-jsx",    // 确保 JSX 配置为 react-jsx（适用于 React 17+）
    "moduleResolution": "node"  // 确保模块解析策略为 node
  },
  "include": ["src"]
}
